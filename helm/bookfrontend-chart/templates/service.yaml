apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookfrontend-chart.fullname" . }}
  labels:
    {{- include "bookfrontend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookfrontend-chart.selectorLabels" . | nindent 4 }}
